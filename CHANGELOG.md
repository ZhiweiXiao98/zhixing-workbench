# 更新记录

## 0.6.15 - 2026-08-14

- Windows 更新安装改用套件内置解压器，包含中文文件名的正式 Release 可以正常安装。

## 0.6.14 - 2026-08-14

- 知识证据校验兼容当前 Codex、ChatGPT 与飞书事件编号，不再误报来源不一致。
- 日期开头的完整经历段落不再被误判为编号列表。
- 中文或含空格的项目名称可以正常写入知识页面。

## 0.6.13 - 2026-08-14

- Obsidian 内的飞书同步和手动整理改用安装时记录的 Node.js，避免把桌面程序误当作运行程序而误报失败。

## 0.6.12 - 2026-08-14

- 飞书同步部分成功时显示真实新增数量和待重试模块，不再把已同步内容统一提示为失败。
- 没有录制文件的会议仍正常进入知行台，录制仅作为可选补充内容。
- 已发起审批改用专用只读接口，会议搜索仅在飞书明确表示还有下一页时继续翻页。

## 0.6.11 - 2026-08-14

- 飞书预览页按实际授权范围判断所选模块是否缺权，权限齐全时显示“所选模块权限已就绪”。
- 已授权用户不再看到无条件出现的“补充所选模块权限”，避免反复进入授权流程。
- 新增模块确实缺少只读权限时，才在选择页和预览页提供补充入口。

## 0.6.10 - 2026-08-14

- 多维表格候选项改用最小权限的数据表列表接口，点击“选择”后可继续选择数据表和视图。
- 飞书应用缺少 Base 只读权限时，在 Obsidian 内显示真实原因和官方开通入口，不再提示模糊的“稍后重试”。
- 权限入口一次预选表、视图和记录三项只读权限，开通后可在原位置重新读取。

## 0.6.9 - 2026-08-14

- 飞书连接改为 Obsidian 内一次点击发起，浏览器确认后由插件自动完成授权，不再要求第二次确认。
- 未完成个人授权时只显示连接入口，避免把不可用的群聊和多维表格搜索暴露给用户。
- 授权失败或超时后清理本次临时设备码，重新连接会创建新的官方授权流程。

## 0.6.8 - 2026-08-14

- 飞书设置会在选择群聊和多维表格前检查个人授权，并在原页面提供明确的授权入口。
- 未授权时禁用只读搜索，授权完成后可继续按名称或最近使用直接选择。
- 飞书 CLI 的身份、权限和异常错误统一转换为可操作的中文提示，不再暴露用户标识或底层错误详情。

## 0.6.7 - 2026-08-13

- 飞书项目群支持按群名查找，或从最近使用列表中直接选择，不再要求填写群 ID。
- 已保存的项目群继续按群名展示，并保留原有只读采集范围。

## 0.6.6 - 2026-08-13

- 飞书多维表格支持按名称查找，并在候选结果中直接选择数据表和视图。
- 直接粘贴 `/wiki/`、`/base/` 或 Lark 链接时，由官方 `lark-cli` 自动解析，不再要求用户查找内部 ID。
- Base 授权范围补齐名称搜索、数据表和视图所需的只读权限。

## 0.6.5 - 2026-08-13

- 只有明确成功的整理周期才会更新成功判断，部分失败、失败和预算暂停会按退避时间继续重试。
- 升级时自动修复被旧版本误写为等待每日整理的失败状态，并保留失败次数对应的重试间隔。

## 0.6.4 - 2026-08-13

- 保留知识事务部分失败的主题标题、具体校验原因和 stdout 回执，避免后台只显示 `node exit 2`。
- 后台错误状态显示失败批次明细，并在退避到期后的成功重试中恢复 `last_success`。
- 增加后台入口的 Codex Desktop 状态与成对增量事件回归验证。

## 0.6.3 - 2026-08-13

- 增加 Codex Desktop 结构化会话增量采集，按生产者版本、文件游标和稳定事件 ID 兼容读取并去重。
- 将 Codex CLI Hook、桌面采集、网页接收、知识执行器和飞书拆分为事实型健康状态，显示探活、最后事件、断流和错误。
- 修复首次没有整理记录时无法补跑的问题；保留每天 23:30，并持久化尝试、成功、下次执行和失败退避。
- 手动“立即整理”只依赖知识运行时与实际探活的 Codex 执行器，采集断流不再阻止整理已有队列。
- Windows 增加 Codex Desktop 稳定运行目录发现，每个候选必须通过 `--version` 探活。
- Codex Desktop 数据契约仅接受已验证的 0.144 与 0.147 系列；未知版本停止推进游标并显示兼容错误。
- 整理进程中断后会把陈旧运行状态恢复为可见退避，避免重启后永久卡住。
- 增加套件自有的单一后台调度入口，登录后独立于 Obsidian 守候 23:30，睡眠或离线恢复后补跑。
- 后台宿主、Obsidian 与手动整理共用跨进程租约，避免重复采集、重复整理和重复常驻进程。

## 0.6.2 - 2026-08-10

- 修复 Windows 旧计划任务 XML 缺少 `Enabled` 时被误判为禁用的问题，并在 0.6.1 安装上自动纠正恢复状态。
- 迁移时精确移除并备份旧 `obsidian-capture.ps1` Hook；保留无关 Hook，卸载时安全恢复并保护用户修改。

## 0.6.1 - 2026-08-06

- 修正 Release 根目录的 Windows、macOS 和 Ubuntu 安装入口，并让三端 CI 解包真实资产执行安装生命周期。
- 增加 Codex Skill 所有权、首次安装备份、失败恢复、用户修改冲突保护和 0.6.0 安全认领升级。
- 将浏览器扩展固定到设备配置目录，安装、诊断、Obsidian 入口、文档和更新统一使用同一路径。
- 增加私人版 0.5.0 插件、接收密钥和已确认旧计划任务的可逆迁移，保留全部个人数据。
- 增加 ChatGPT DOM 采集冒烟测试，以及 macOS、Ubuntu 桌面进程常见 Codex 与 `lark-cli` 路径发现。

## 0.6.0 - 2026-08-06

- 首次公开发布知行台套件。
- 支持 Windows x64、macOS Apple Silicon、macOS Intel 和 Ubuntu x64。
- 提供 Obsidian 工作台、Codex 与 ChatGPT 自动采集、主题队列、双层 Wiki 和事务重试。
- 提供默认关闭的飞书只读连接器，覆盖任务、日程、会议纪要、文档/Wiki、指定 Base、审批结果和筛选后的项目群消息。
- 提供安装、诊断、更新、回滚、卸载、隐私扫描与 GitHub Release 流程。
