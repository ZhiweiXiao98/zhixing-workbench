# 项目协作说明

## 范围

- 本仓库是知行台套件唯一公开发布源，使用全新 Git 历史。
- 只包含程序、匿名示例和公开文档，不引入任何真实 Vault、设备配置或凭据。
- 跨平台逻辑优先放在 Node 运行时或 Obsidian 桌面插件，系统脚本保持薄层。

## 开发

- 使用项目自身依赖，Node.js 版本以 `.nvmrc` 为准。
- 套件、Obsidian 插件、浏览器扩展和 Codex 插件共用同一语义版本。
- 更新只能替换程序文件；`raw/`、`wiki/`、`成果/`、手写内容和设备配置永不进入更新清单。
- 版权声明统一使用 `Copyright (c) 2026 肖志伟`。

## 验证

- 优先运行与改动直接相关的聚焦测试，完整门禁在发布前运行一次。
- 发布前扫描工作树和完整 Git 历史，阻止私人路径、凭据和真实项目材料进入资产。
- 安装、更新、回滚和卸载使用独立临时 Vault 验证，不修改真实用户数据。
